// CAPABILITY: credits_value = 15

// Speed boost item - allows multiple moves per turn
pub fn apply_speed_boost() {
    // In a future version, this could enable multiple moves per turn
    // For now, it's just a valuable collectible
}

pub fn get_speed_multiplier() -> f32 {
    1.5 // 50% speed increase (theoretical)
}